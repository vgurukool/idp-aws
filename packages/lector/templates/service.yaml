apiVersion: v1
kind: Service
metadata:
  name: {{ include "lector.fullname" . }}
  labels:
    {{- include "lector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
    {{- if .Values.sso.enabled }}
    - name: oauth2-proxy
      port: {{ .Values.sso.port | default 4180 }}
      targetPort: 4180
      protocol: TCP
    {{- end }}
  selector:
    {{- include "lector.selectorLabels" . | nindent 4 }}
